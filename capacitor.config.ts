import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pruebatecnicafront.app',
  appName: 'prueba-tecnica-frontend',
  webDir: 'dist/prueba-tecnica-frontend/browser'
};

export default config;
